// Global Vitest setup. Runs before every test file.
//
// Keep this lean: only things that genuinely need to be global belong here.
// Per-test mocking lives in the test file itself.

import "@testing-library/jest-dom/vitest";
